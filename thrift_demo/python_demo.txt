1、传了重复的值的处理，SDK可以增加参数，提供如有重复的值是替换还是累计
(消息格式是字典，不会出现重复值。)


2、SDK设置错误级别，DEBUG/INFO/ERROR，并且定义LOG格式，可以在console上与业务的LOG进行区分
（已经提供错误日志，保存在sdk中）


3、log信息应写入自己的包内


4、返回信息为空，应定义一些常量错误码


5、给项目定义一个代号，SDK包名更换成代号名称